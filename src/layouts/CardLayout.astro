---

---

<main>
  <slot />
</main>

<style>
  main {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    grid-template-rows: repeat(3, 1fr);
    padding: var(--space-l);
    gap: var(--space-m);
  }

  .last-widget {
    grid-row: 3;
    grid-column: 1 / -1;
  }
</style>